
class Constants {
  Constants._();

  /// used for (app folder, send to server)
  static const appName = 'VosateZehn';
  /// used for (app title)
  static String appTitle = 'vosate zehn';
  static String appVersionName = '5.2.2';                     //match by pubspec.yaml
  static int appVersionCode = (5 * 100) + (2 * 10) + (2);
}
