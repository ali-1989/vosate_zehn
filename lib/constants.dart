
class Constants {
  Constants._();

  /// used for (app folder, send to server)
  static const appName = 'amlak';
  /// used for (app title)
  static String appTitle = 'amlak';
  static String appVersionName = '0.0.1';                     //match by pubspec.yaml
  static int appVersionCode = (0 * 1000) + (0 * 100) + (1);
}
