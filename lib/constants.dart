
class Constants {
  Constants._();

  /// used for (app folder, send to server)
  static const appName = 'amlak';
  /// used for (app title)
  static String appTitle = 'amlak';
  static String appVersionName = '1.0.5';         //match by pubspec.yaml
  static int appVersionCode = (1 * 1000) + (0 * 100) + (5);
}
